import { InformationInterceptor } from './information.interceptor';

describe('InformationInterceptor', () => {
  it('should be defined', () => {
    expect(new InformationInterceptor()).toBeDefined();
  });
});
